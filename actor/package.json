{
  "name": "opensoon-la",
  "version": "0.1.0",
  "description": "Los Angeles pre-opening business lead radar for Apify",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/main.js",
    "test": "node --test tests/*.test.js",
    "check": "node --check src/main.js && node --check src/core.js && node --check src/socrata.js"
  },
  "dependencies": {
    "apify": "^3.7.0"
  }
}
